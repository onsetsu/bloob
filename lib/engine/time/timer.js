mini.Module(
	"engine/time/timer"
)
.requires(
	"engine/loop"
)
.defines(function(Loop) {
	var Timer = mini.Class.subclass({
		initialize: function() {
			
		},
		update: function() {
			
		}
	});
	
	return Timer;
});
